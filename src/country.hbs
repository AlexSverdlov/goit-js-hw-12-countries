<div class="info">
    <h1>{{name}}</h1>
    <p>Capital:{{capital}}</p>
    <p>Population:{{population}}</p>
    <p>Languages:</p>
    <ul>
        {{#each languages}}
        <li>{{name}}</li>
        {{/each}}
    </ul>
</div>
<div>
    <img class="flag" src="{{flag}}" alt="{{name}}" />
</div>